# Hero media

The home page expects `aab-hero-parallax.mp4` in this directory.

## Delivery contract
- Short seamless cinematic/parallax loop made from the approved barn exterior image.
- No audio.
- HTML playback flags: `autoplay muted loop playsinline preload="none"`.
- The existing `images/IMG_1415.jpg` is the poster and hard fallback.
- For production, an ImageKit CDN URL may replace the local MP4 path after upload/transcoding.
- Keep large source masters outside this repo; only ship an optimized web rendition here.
